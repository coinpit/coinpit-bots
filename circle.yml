machine:
  node:
    version: 4.0.0
  services:
    - docker

test:
  override:
    - npm test

deployment:
  docker:
    branch: master
    commands:
      - docker info
      - docker build -t coinpit/coinpit-bots:1.0.0 .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push coinpit/coinpit-bots
